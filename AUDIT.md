# 🔍 GIGA Standard v5 監査：ふりかえりジャーナル

> **履歴資料:** この文書のGAS計測値は旧構成の記録です。現在の本番構成はGitHub Pages + Google Drive APIへ完全移行しています。現行の検査結果は `npm run verify`、設計は `docs/DRIVE_NATIVE_ARCHITECTURE.md` を参照してください。

- **監査日**: 2026-08-08
- **監査時のコミット**: `b781dbd`（`main`）
- **改修後の再測定**: 同日（P0.5 / P1 適用後）
- **アーキテクチャ型**: **C+型**（GitHub Pages シェル + GAS 2デプロイ）

> **この文書の読み方**
> 「実測」の列は**改修前**の値です。改修後の再測定は末尾の「改修後の再測定」にまとめてあります。
> 判定欄の `❌ → ✅` は、改修後に基準を満たしたことを示します。

> **2026-08-13 追補:** 先生画面のQRコードを外部サービス依存からブラウザ内生成へ変更しました。児童用URLを第三者へ送信せず、QR生成コードは先生ポータルだけに配信します。`npm test` でQRの生成・再現性を、品質ゲート B8 で外部QRサービスへの回帰と条件付き読み込みを検査します。

## 測り方（推測で書かないための前提）

| | 内容 |
| --- | --- |
| 実ブラウザ | Chromium（Playwright）1366×768 と 320×568 |
| 色の読み取り | 1px 塗って `getImageData` で読む方式。グラデーションは**色停止をすべて解いて最悪値**を採る（§7-2） |
| GAS 本体の測り方 | `index.html` の GAS テンプレート（`<?= bootMode ?>` 等）を実体化し、`google.script.run` に戻り値の見本を与えて本編まで進めた（§7-3）。**リポジトリには手を入れず、検査用の複製だけ**を使用 |
| CDN | 作業環境から `unpkg.com` / `cdn.jsdelivr.net` / `cdn.tailwindcss.com` へ**到達できない（HTTP 000）**。npm から同じ版の実バイトを取って手元に並べ、**検査用の複製だけ**を向け直した（§7-4）。Google Fonts は塞がれたまま測定（学校と同じ状態） |
| 測った画面 | 児童：提出画面／ヒント展開　先生：ジャーナル管理／心のバイタル／クラス設定／返却モーダル |

---

## 総括

**動作をいちばん脅かしているのは表示崩れではなく、依存です。**
GAS 本体（`index.html`）が React・Babel・Tailwind をすべて CDN から読んでおり、**学校のフィルタリングで1本でも塞がれると、児童の画面は白いまま何も出ません**。
しかも `@babel/standalone` は **実測 2,997.6 KB**（約3MB）で、その仕事は「開くたびに JSX を翻訳し直すこと」です。

一方、**`docs/` のシェル（PWA）は良好**でした。過去の改修が効いており、Service Worker・更新通知・オフライン・他アプリのキャッシュ保護・maskable アイコンはすべて基準を満たしています。

| 優先度 | 内容 | 実測 |
| --- | --- | --- |
| **P0.5** | ブラウザ内 Babel / CDN 依存（起動しない事故） | 初回 JS **3,153.9 KB**、うち Babel 2,997.6 KB |
| **P1** | ふりがな（`rt`）の色を決め打ち | 提出ボタン上で **比 1.36**、おへんじ帯の上で **比 1.08** |
| **P1** | 拡大禁止（`user-scalable=no`）が2か所 | `index.html:5` と `Main.gs:154` |
| **P1** | コントラスト未達 | 延べ **199件**（色の組み合わせ 33種） |
| **P1** | タップ領域 44px 未満 | 延べ **101件**（34種）。最小 **16×16px** |
| **P1** | GAS 本体に `prefers-reduced-motion` / `forced-colors` が無い | 0件（`docs/` 側にはある） |
| **P1** | CSP なし / SRI なし | 0件 |
| **P3** | CI ワークフローなし | `.github/workflows` が存在しない |

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT・リポジトリ直下に実体あり |
| A2 | .gitignore | ✅ | `node_modules/` `.clasp.json` `.env` を含む |
| A3 | dependabot.yml | ✅ | `.github/dependabot.yml` あり |
| A4 | README.md / MANUAL.md / AUDIT.md | ✅ | 3点とも存在（AUDIT.md は本ファイル。README/MANUAL は PR #7 で整備済み） |
| A5 | CI ワークフロー | ❌ | `.github/workflows/` が無い。PR 時点で落ちていることに気づけない |

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ❌ | `Content-Security-Policy` の宣言 **0件**（`index.html` / `docs/*.html` とも） |
| B2 | 秘密情報・IDの直書きなし | ✅ | `docs/config.js` の exec URL と GIS クライアントIDは**公開前提の値**。APIキー・シートID・メールアドレスの直書きは無し。`.clasp.json` / `.env` のコミットも無し |
| B3 | OAuthスコープ最小 | ✅ | `spreadsheets` / `script.external_request` / `userinfo.email` の3つのみ。`auth/drive`（全体）なし |
| B4 | postMessage の宛先が `*` でない | ⚠️ | `index.html:91` `var target = window.BOOT.shellOrigin \|\| '*';` … `sp_shellUrl` 未設定時に `*` へ落ちる。受信側は origin 検証あり（`index.html:111` / `docs/index.html:374`） |
| B5 | サーバー側5段ガード | ✅ | `guardMember_`（Tenant.gs:66）で ①トークン検証 ②テナント解決 ③名簿照合 を実施。④`assertOwner_` ⑤`assertRowOwner_` も実装済み |
| B6 | CDN から取る実行コードが 0 | ❌ | **5本**（React / ReactDOM / Babel / Tailwind / canvas-confetti）。**合計 3,153.9 KB** |
| B7 | 残る外部資産に SRI と版の固定 | ❌ | `integrity=` **0件**。`@babel/standalone` と `cdn.tailwindcss.com` は版すら固定されていない |

### B6 の内訳（実測・非圧縮）

| 資産 | 取得元 | 実バイト |
|---|---|---:|
| `@babel/standalone/babel.min.js` | unpkg | **2,997.6 KB** |
| `react-dom@18` (umd, production) | unpkg | 128.7 KB |
| `canvas-confetti@1.6.0` | jsDelivr | 17.1 KB |
| `react@18` (umd, production) | unpkg | 10.5 KB |
| Tailwind | cdn.tailwindcss.com（ブラウザ内で CSS 生成） | 別途（使用クラスのみ生成すると **42.8 KB**） |
| **合計（JS）** | | **3,153.9 KB** |

`index.html:232` が `<script type="text/babel">` で、**約1,400行の JSX を開くたびにブラウザ内でコンパイル**しています。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally（GAS） | ✅ | `withScriptLock_`（Main.gs:101）が `finally` で解放。`opCreateTenant` / `opAdoptTenant` は `getUserLock` も併用 |
| C2 | 自動復旧（シート再生成） | ✅ | `ensureTenantSheets_` / `createSheetIfNotExists_` で必須シート・状態列を補完 |
| C3 | pagehide で記録確定 | ⚠️ | `pagehide` は未使用。ただし下書きは**入力のたびに** localStorage へ書いており（`index.html:418`）、タブ破棄でも直前まで残る |
| C4 | 通信失敗時のリトライと明示 | ✅ | `TOKEN_EXPIRED` は自動再取得して1回リトライ、`LOCK_BUSY` は指数バックオフで最大3回（`index.html:203-226`）。失敗はトーストで明示 |
| C5 | localStorage.clear() を使っていない | ✅ | 0件。キーは匿名 uid 接頭辞（`draft_<uid>` / `read_journals_<uid>`） |
| C6 | `caches.keys()` の全削除をしていない | ✅ | `docs/sw.js:56` で `startsWith(CACHE_PREFIX)` に限定。**実測で他アプリのキャッシュ2件が残ることを確認** |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover（GAS は Main.gs も） | ❌ | `index.html:5` にはあるが、**`Main.gs:154` の `addMetaTag` に無い**。§5 の「両方直す」に該当 |
| D2 | 100dvh を使用 | ✅ | `index.html:44` `@supports (height: 100dvh)` で `.h-screen` を上書き。`docs/offline.html:40` のみ `100vh` 単独 |
| D3 | safe-area-inset を適用 | ✅ | 10件（`docs/index.html` の iframe・下部バー・更新帯） |
| D4 | clamp() による fluid type | ⚠️ | `docs/index.html:126` に `--fs-body` / `--fs-note` あり。**GAS 本体（児童・先生の画面）は固定サイズのみ**で clamp なし |
| D5 | Canvas に devicePixelRatio 補正 | — | **該当なし**。`getContext('2d')` は画像圧縮（`index.html:275`）の1か所のみで、画面に描画する Canvas は無い |
| D6 | 320px 幅で横スクロールが出ない | ✅ | 児童画面 `scrollWidth=320` / 先生画面 `scrollWidth=320`。**横スクロールなし** |
| D7 | 画像に width/height、150KB以下 | ⚠️ | PNG 5点はすべて 150KB 以下（最大 12.5KB）。`<img>` 3か所のうち `width/height` があるのは QR の1件のみ、**`alt` も QR の1件のみ** |
| D8 | コントラスト 4.5:1 以上 | ❌ | **延べ 199件が未達**（色の組み合わせ 33種）。最悪 **1.08** |
| D9 | タップ領域 44px 以上 | ❌ | **延べ 101件が未達**（34種）。最小 **16×16px**（クラス設定のチェックボックス） |
| D10 | prefers-reduced-motion | ❌ | **GAS 本体に 0件**。`docs/` 側のみ実装（しかも正しく `.01ms`）。児童画面には紙吹雪・バウンス・パルスがあり、感覚過敏の配慮が効かない |
| D11 | forced-colors | ❌ | **GAS 本体に 0件**。`docs/` 側のみ実装 |
| D12 | 提示モード | — | 一斉提示を前提としない設計（各自の端末で書く）。**要判断** |
| D13 | 印刷CSS | ⚠️ | 印刷帳票は `openPrintView`（`index.html:741`）が別ウィンドウを生成。**`@page` 指定が無い**ため用紙サイズ・余白がブラウザ既定まかせ。`page-break-after` は指定済み |
| D14 | 拡大を禁止していない | ❌ | **2か所で禁止**。`index.html:5` と `Main.gs:154` の両方に `maximum-scale=1.0, user-scalable=no` |

### D8 の内訳（比の低い順・色の組み合わせ単位）

| 比 | 必要 | 文字色 | 背景 | サイズ | 延べ | 例 |
|---:|---:|---|---|---:|---:|---|
| **1.08** | 4.5 | `rgb(107,114,128)` | `rgb(99,102,241)` | 10.8px | 4 | おへんじ帯の**ふりがな**「せんせい」「とど」 |
| **1.36** | 4.5 | `rgb(107,114,128)` | `rgb(234,88,12)` | 10.8px | 2 | **提出ボタンのふりがな**「ていしゅつ」 |
| 1.46 | 3 | `rgb(209,213,219)` | `rgb(254,254,254)` | 24px | 6 | ヘッダーの区切り「\|」（装飾） |
| 1.92 | 4.5 | `rgb(234,179,8)` | 白 | 12px | 12 | ヒントの「＋」 |
| 2.26 | 4.5 | 白 | `rgb(251,146,60)` | 18px | 4 | **「提出する」ボタンの文字** |
| 2.39〜2.54 | 4.5 | `rgb(156,163,175)` | 白 | 10〜14px | 115 | カレンダーの日付・曜日、表の見出し、空状態の案内文、フッター |
| 2.54 | 4.5 | 白 | `rgb(16,185,129)` | 14px | 3 | 「一括返却」「承認」ボタン |
| 3.35〜3.96 | 4.5 | 各種 | 各種 | 12〜18px | 15 | 「今日のテーマ」ラベル、💌 バナー、「保存」「全データを削除」 |
| 4.32〜4.45 | 4.5 | `rgb(107,114,128)` | 淡色地 | 7〜12px | 22 | 小さなふりがな、名簿の表見出し |

> **いちばん困っている人に向けた文が、いちばん読みにくい**という §2-8 の指摘がそのまま出ています。
> ふりがなが必要なのは低学年の児童であり、その**ふりがなが最悪値（1.08 / 1.36）**でした。

### D9 の内訳（小さい順）

| 実測 | 要素 | 場所 |
|---|---|---|
| **16×16px** | `<input type="checkbox">` | クラス設定「参加を受け付ける」「参加は先生の承認制にする」 |
| 49×16px | `<a>`「GIGA山」 | フッター（§Phase3 が名指しで注意している箇所） |
| 32×32px | `<button>` | 児童画面のカレンダー前月/翌月 |
| 35〜38×32px | `<button>` 😊👏💪⭐ | 先生画面のスタンプ即返却 |
| 36×36px | `<button>` | ヒントパネルの閉じる |
| 72×21px | `<select>` | 名簿の役割 |
| 142×16px | `<label>` 月〜金曜日 | 週間スケジュール |
| 88〜94×40px | `<select>` | 状況フィルタ・クラス切り替え |
| 115×38px | `<button>` | テンプレート4種（YWT/KPT/5W1H/3つのきづき） |

児童画面だけで延べ **17件**、先生画面で **84件**。

## E. PWA（Part I §3）

**ここは良好です。** 過去の改修が効いています。

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | 3つとも `/Reflection_Journal/` |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | 192/512/maskable 192/512 + `icon-180.png`。**`icon-180.png` の透明画素 0.0%（alpha 最小 255）** |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `docs/index.html:22` で `pwa-install-hook.js` を同期読み込み（`<head>` の実質最初） |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | `#install-bar`。iOS では手順案内に切り替え、14日間の再表示抑止つき |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測**：`qalc-shell-v3` / `kanji-town-v1` を置いて版を上げた後も**両方残存**。自分の旧版 `rj-shell-v5` のみ削除 |
| E6 | sw.js が localStorage に触れていない | ✅ | 0件（注意書きのコメントのみ） |
| E7 | 更新通知（押すまで切り替わらない） | ✅ | **実測**：版を上げて `update()` → **3秒放置しても `waiting` のまま・画面遷移は増えない**。帯「あたらしい バージョンが あります／さいしんに する／あとで」表示。押下後に切り替わり再読み込み（遷移 1→2） |
| E8 | 初回訪問で勝手にリロードしない | ✅ | **実測：画面遷移 1回**。`controllerchange` を購読していないため §3-3 の事故は起きない |
| E9 | Service Worker が実際に登録されている | ✅ | **実測**：`getRegistration()` が有効な登録を返す（scope = シェル直下） |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | **実測**（サーバーを実際に停止）：①圏外でシェルが起動する ②本体キャッシュを消すと `offline.html`（「いま インターネットに つながっていません」）が出る。ページは自前CSSのみ・JS 不使用 |
| E11 | APP_VERSION を今回のリリース値に更新した | — | 今回はコード未変更のため対象外（現行 `v5`） |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **実測 0.000%**（192/512 とも）。中央80%の円の外側に、下地オレンジ以外の画素は1つも無い |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ⚠️ | MANUAL.md に「ホーム画面に追加するとアプリのように起動できる」旨はあるが、**共有ボタンからの手順は画面側の案内のみ**でマニュアルに図解が無い |
| E14 | 更新の帯に `.no-print` が付いているか | ❌ | 付いていない（§2-12 の「あとから足した固定要素にも `.no-print`」に該当）。ただし現状シェルには印刷CSSが無いため影響は限定的 |

> **注意（測定上の制約）**: シェルは `location.protocol === 'https:'` のときだけ SW を登録します（`docs/index.html:437`）。
> そのため 127.0.0.1 では登録されず、**検査用の複製だけ** localhost を許可して測りました。本番（GitHub Pages / https）では条件を満たします。

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ❌ | GAS 本体：`<img>` 3件中 `alt` は1件のみ、`aria-label` **0件**、`aria-live` / `role="alert"` **0件**（トーストが読み上げられない）。`docs/` 側は更新帯に `role="status" aria-live="polite"` あり |
| F2 | モーダルに role="dialog"・Esc で閉じる | ❌ | モーダル3種（確認ダイアログ・過去の記録・返却画面）に `role="dialog"` / `aria-modal` **0件**、Esc ハンドラ **0件**、フォーカストラップ無し |
| F3 | キーボードのみで全機能に到達 | ⚠️ | **未計測**（Tab 順の走査は未実施）。ただし先生画面の本文ハイライトは `onMouseUp` / `onTouchEnd` によるテキスト選択が唯一の手段で、**キーボード代替が無い**ことは静的に確認 |
| F4 | rt の色を決め打ちしていない | ❌ | `index.html:293` `<rt className="... text-gray-500 ...">` で決め打ち。**実測 65個の `rt` のうち 16個が 4.5 未満、最悪 1.08** |
| F5 | 初回JS 300KB以下 | ❌ | **3,153.9 KB**（目標の約10.5倍）。うち Babel が 2,997.6 KB |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `index.html` の 1,637行 / 120.8KB |
| F7 | ルビに `<rp>` を添えている | ❌ | `<rp>` **0件**（読み上げでふりがなが二重に読まれる） |
| F8 | JS エラー・読み込み失敗 | ✅ | **実測 0件**（児童画面・先生画面とも。CDN を手元の控えに向けた状態で） |

## G. 学習ログ（学習系のみ）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1/G2 | `study.v1` | — | **該当なし**。本アプリは記録をクラスのスプレッドシートに保存する設計で、`study.records.v1` は使用しない |

---

## 測っていないもの（「未計測」を ✅ と書かないための明示）

1. **本番環境（`gigayama.github.io` / `script.google.com`）での動作** — 作業環境から到達できません（プロキシで遮断）。デプロイ差分・実 GAS の応答・実機の GIS サインインは未確認です。
2. **実機での表示** — Chromebook・iPad・電子黒板の実機は未使用。すべて Chromium のエミュレーションです。
3. **Tab 順と読み上げ（F3）** — キーボードのみの到達性、スクリーンリーダーでの読み上げ順は未計測です。
4. **`Main.gs` の `addMetaTag` が実際に返す viewport** — サーバー側処理のため手元では再現できません。**コード上の値**（`Main.gs:154`）で判定しています。
5. **印刷の実出力** — `openPrintView` が開く帳票の実際の印刷プレビュー（用紙・改ページ）は未計測です。
6. **iOS Safari 固有の挙動** — ITP による localStorage 失効、`apple-touch-icon` の実表示は未計測（画像の透明有無のみ実測）。
7. **Gemini API の実応答** — APIキーが無いため AI 機能は未実行です。
8. **`cdn.tailwindcss.com` が実際に生成する CSS の量** — 到達できないため、同等構成を tailwindcss 3.4.17 でビルドした **42.8 KB** を代替値としています。
9. **改修前後の比較** — 本監査は現状の測定のみです。「以前は壊れていた」といった比較は行っていません。

---

## 改修後の再測定（P0.5 / P1 適用後）

測り方は改修前とまったく同じ（同じ probe・同じ6画面・同じ2つの画面幅）です。

### 依存（P0.5）

| 項目 | 前 | 後 |
| --- | ---: | ---: |
| 初回 JS | 3,153.9 KB | **271.0 KB** |
| うち `@babel/standalone` | 2,997.6 KB | **0** |
| CDN から取る実行コード | 5本 | **0本** |
| CDN を遮断した状態で児童画面が出るか | 出ない | **出る** |
| 総アセット（初回） | — | 335.9 KB（目標 1MB 以下） |

> 「CDN 遮断」は Playwright で `unpkg.com` / `cdn.jsdelivr.net` / `cdn.tailwindcss.com` /
> `fonts.googleapis.com` / `fonts.gstatic.com` へのリクエストを全部 abort した状態です。
> この状態で今日のテーマ・入力欄・カレンダーが表示され、JS エラーは 0 件でした。

### 表示・アクセシビリティ（P1）

| 項目 | 前 | 後 |
| --- | ---: | ---: |
| コントラスト 4.5:1 未満（延べ） | 199件 | **0件** |
| 最悪のコントラスト比 | 1.08 | **4.75** |
| ふりがな（`rt`）で 4.5 未満 | 16 / 65個 | **0 / 65個** |
| タップ領域 44px 未満（延べ） | 101件 | **0件** |
| 最小のタップ領域 | 16×16px | **44×44px** |
| 320px での横スクロール | なし | なし |
| JS エラー | 0件 | 0件 |
| 拡大禁止の箇所 | 2か所 | **0か所** |
| `viewport-fit=cover` のある viewport 宣言 | 4 / 5 | **5 / 5** |

画面ごとの内訳（1366×768 / コントラスト・タップの順）:

| 画面 | 前 | 後 |
| --- | ---: | ---: |
| 児童：提出画面 | 61件 / 8件 | 0件 / 0件 |
| 児童：ヒント展開 | 76件 / 9件 | 0件 / 0件 |
| 先生：ジャーナル管理 | 9件 / 30件 | 0件 / 0件 |
| 先生：心のバイタル | 20件 / 3件 | 0件 / 0件 |
| 先生：クラス設定 | 21件 / 20件 | 0件 / 0件 |
| 先生：返却モーダル | 12件 / 31件 | 0件 / 0件 |

### 壊れていないことの確認（実際に操作して測った）

| 確かめたこと | 結果 |
| --- | --- |
| CDN 遮断下で児童画面が出る | 出る（今日のテーマ・入力欄・カレンダーを確認） |
| 返却モーダルが開く / `role="dialog"` と `aria-modal` を持つ | はい |
| Esc で返却モーダルが閉じる | 閉じる |
| Esc で確認ダイアログが閉じる | 閉じる |
| 名簿の行追加が動く | 4行 → 5行 |
| 「動きを減らす」設定で紙吹雪が出ない | 通常 1回 / reduce 0回 |
| JS エラー | 0件 |

### アイコン（差し替え後）

| ファイル | 大きさ | 透明画素 | セーフゾーン外の中身 |
| --- | ---: | ---: | ---: |
| `icon-180.png`（apple-touch） | 10.4 KB | **0.0%** | — |
| `icon-192.png` | 7.0 KB | 9.0% | — |
| `icon-512.png` | 27.5 KB | 8.6% | — |
| `icon-maskable-192.png` | 10.7 KB | 0.0% | **0.160%** |
| `icon-maskable-512.png` | 44.3 KB | 0.0% | **0.196%** |

### 品質ゲート

`npm run check` の17項目が通ることに加えて、**わざと壊して落ちることを10項目で確認**しました
（`node scripts/selftest-check.mjs`）。この確認をしたことで、ふりがなの検査に
「継がせる受け皿を1つ外しても落ちない」穴があることが分かり、直しています。

### まだ残っているもの

| 項目 | 状態 | 理由 |
| --- | --- | --- |
| B1 CSP | ❌ のまま | GAS は本体を `<script>` として差し込む構成のため、`script-src 'self'` を入れると全部止まる。GAS 固有の検討が要る（次のフェーズ） |
| B7 SRI | — | 外部から読む実行コードが 0 になったため、対象が無くなった（Google Fonts はスタイルのみ） |
| A5 CI | ✅ | `.github/workflows/ci.yml` を追加（`pull_request` と `push` の両方で動く） |
| D4 fluid type | ⚠️ のまま | GAS 本体は固定サイズのまま。次のフェーズ |
| D12 提示モード | — | 各自の端末で書く設計のため不要と判断 |
| D13 印刷 | ⚠️ → 一部改善 | 画面側に `@page` を入れた。帳票（別ウィンドウ）側は未対応 |
| E13 iOS の追加手順 | ⚠️ のまま | MANUAL に図解が無い |
| F3 キーボード到達性 | **未計測** | Tab 順の走査は行っていない |

---

## 提案する修正の順序

| フェーズ | 内容 | 対象ファイル | 備考 |
|---|---|---|---|
| **P0** | CI ワークフロー追加（`pull_request` と `push` の両方） | `.github/workflows/ci.yml` | 破壊リスクなし |
| **P0.5** | ブラウザ内 Babel / CDN 依存の解消（ビルド時生成へ） | `index.html`・新規 `src/`・`tools/` | **アーキテクチャ変更**。§停止条件により**マージは人間に委ねる** |
| **P1-a** | ふりがな（`rt`）の色（比 1.08 / 1.36） | `index.html` | 単独で効果が大きく、リスクが小さい |
| **P1-b** | 拡大禁止の解除・`viewport-fit=cover` の追加 | `index.html`・`Main.gs` | **GAS 本体**のため PR 提案・テストデプロイ依頼 |
| **P1-c** | コントラスト（199件）・タップ領域（101件） | `index.html` | 色相は変えず1〜2段濃くする。**変更前後の比を PR に記載** |
| **P1-d** | `prefers-reduced-motion` / `forced-colors` を GAS 本体へ | `index.html` | 紙吹雪の抑止を含む |
| **P1-e** | a11y（`role="dialog"`・Esc・`aria-live`・`alt`・`<rp>`） | `index.html` | |
| **P1-f** | CSP + SRI | `index.html`・`docs/*.html` | **P0.5 の後**。先に外部ファイル化しないと `'unsafe-inline'` 頼みになる |
| **P2** | 画像・maskable | — | **すでに基準内**。対応不要 |
| **P3** | `@page` の追加、更新帯の `.no-print`、MANUAL に iOS 手順の図解 | `index.html`・`docs/index.html`・`MANUAL.md` | |
| **P4** | 品質ゲート（`scripts/check-project.mjs` ほか） | 新規 | **わざと壊して通ることを確認**したうえで導入 |

### 人間に確認してほしいこと

1. **GAS 本体（`.gs` / `index.html` / 生成物）の変更は、本番でテストデプロイして確認してください**（作業環境から `script.google.com` へ到達できないため、こちらでは確認できません）。
   確認する順番:
   1. `clasp push`（または手貼り）で `vendor.html` / `css.html` / `app.html` / `index.html` / `Main.gs` を反映する
   2. **デプロイ A / B 両方**を「デプロイを管理 > 編集 > 新バージョン」で更新する
   3. 児童用URLを開き、画面が出ること・提出できることを見る
2. **CSP（B1）は今回入れていません。** GAS は本体を `<script>` として差し込む構成のため、`script-src 'self'` を入れると `google.script.run` を含めて全部止まります。入れるなら GAS 固有の検討が必要です。
3. **提示モード（D12）が必要か。** 現状は各自端末で書く設計のため、不要と判断しています。
