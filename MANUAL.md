# 📖 ふりかえりジャーナル 操作マニュアル

## このアプリについて

先生と児童が [共通URL](https://reflection-journal.giga-school.com/) からGoogleアカウントで利用する振り返りアプリです。学校ごとのGASデプロイやスプレッドシート作成は必要ありません。

記録は運営者のサーバーではなく、先生・児童それぞれのGoogle Driveに保存されます。

- 児童の本文と画像：児童のDriveに保存し、先生へ閲覧共有
- 先生のテーマとおへんじ：先生のDriveに保存し、対象児童へ閲覧共有
- クラス設定と名簿：先生のDriveに保存し、児童へは共有しない

利用にはインターネット接続、Google Driveを使えるアカウント、対応ブラウザが必要です。学校アカウントでは、Workspace管理者がこのアプリのOAuthクライアントとDrive共有を許可している必要があります。

## 最初のGoogleログイン

1. 共通URLまたは先生から届いた専用URLを、Chrome、Edge、Safariなどで開きます。
2. ［Googleアカウントで続ける］を押します。
3. 利用するGoogleアカウントを選び、画面に表示された権限を確認します。
4. 共通URLから開いた場合は、［先生として使う］または［児童として使う］を選びます。専用URLから開いた場合は、児童の参加画面へ進みます。

最初の認可では、ログイン情報と、このアプリが作成したファイルを扱う `drive.file` を求めます。先生または児童の画面へ進む時に、相手から共有されたアプリ専用ファイルを読む `drive.readonly` を別途説明して求めます。Googleが表示する「Drive内のすべてのファイルを表示」という権限範囲を確認してください。アプリ実装上は共有済みかつアプリ固有印の一致するファイルだけを表示します。

### ログイン状態はどこまで続くか

- 安全のため、既定設定ではアクセストークンを端末の保存領域に残しません。同じタブでも再読込みやPWA更新後は再ログインします。
- 前回選んだ役割を復元します。先生は最後に開いたクラス、児童は参加済みクラス一覧へ戻ります。
- タブまたはPWAを終了した場合も認証情報は端末に残しません。
- ブラウザでGoogleへログイン済みなら、通常はパスワードを毎回入力する必要はありません。ただし、Googleまたは学校のセキュリティ設定により、アカウント選択や再認証が表示される場合があります。
- 別のアカウントへ切り替える時は、画面右上のアカウント表示を押して［アカウントを変更］します。

## 先生の使い方

### 1. クラスを作る

1. 共通URLへログインし、［先生として使う］を選びます。
2. クラス名を入力します。
3. ［クラスを作成］を押します。

先生のDriveに、クラス設定JSONが非共有ファイルとして作成されます。クラス名、クラスコード、名簿、テーマ、参加状態などがこのファイルに保存されます。

### 2. 児童を招待する

1. 対象クラスを開き、［クラス設定］を選びます。
2. 必要なら［参加には先生の承認が必要］をオンにして［設定を保存］します。
3. 教室に合う招待方法を選びます。
   - 電子黒板や印刷物：QRコードを表示し、［QRを画像で保存］
   - Google Classroomや学習支援ツール：［専用URLをコピー］
   - メッセージで案内：［招待文をコピー］
   - 確認用：［クラスコードをコピー］
4. ［児童画面を確認］で、配布前の参加画面を別タブで確認できます。

クラスコードだけでクラスを検索することはできません。通常は署名付きの専用URLまたはQRコードを配ってください。招待の既定有効期限は35日です。漏えいした疑いがある場合は［招待URLを失効・再発行］を押し、新しいURLを配布してください。参加済みの児童の記録には影響しません。

### 3. 参加申請と名簿を管理する

児童が専用URLから参加すると、［参加申請・名簿］へ表示されます。

- ［承認］：参加中にし、先生のDriveにその児童専用のおへんじチャンネルを作って児童へ閲覧共有します。
- ［除外］：その児童をクラス画面上で除外します。
- ［1人追加］：名前とGoogleメールアドレスを先に登録します。
- ［名簿をまとめて追加］：1行に `名前, メールアドレス` を貼り付けて一括登録します。

事前名簿に登録した児童は、参加時に同じメールアドレスと照合されます。参加承認をオフにした場合は、児童参加後に先生がクラスを開いた時点で自動承認されます。

### 4. 複数クラスを切り替える

クラス画面上部の［表示するクラス］から別のクラスへ切り替えます。［＋ クラス一覧・新規作成］または［一覧］でクラス一覧へ移動できます。同じ端末では、次回［先生として使う］を開いた時に最後のクラスを開きます。

### 5. テーマを配信する

1. ［ジャーナル管理］を開きます。
2. 適用日と当日のテーマを入力します。
3. 必要なら曜日別テーマも設定します。
4. ［保存して児童へ配信］を押します。

参加中の各児童について、先生所有のおへんじチャンネルへテーマが保存されます。児童画面は共有されたチャンネルを読み込みます。

### 6. 提出を確認する

［ジャーナル管理］では、提出確認を中心に次を使えます。

- 提出率の円グラフと今日の提出状況
- ［すべて／未返却／返却済み］フィルター
- 児童名や本文の検索
- 提出カードから本文、気持ち、添付画像、おへんじ状態の確認
- ［↻ 最新に更新］による即時同期

先生がこの画面を表示している間は、編集を妨げない範囲で約30秒ごとに別端末からの提出を確認します。すぐ確認したい場合は［↻ 最新に更新］を押してください。

### 7. おへんじを返す

短く返す場合：

1. 提出カード下の 😊／👏／💪／⭐ を押します。
2. 返却後に戻したい時は［返却を取り消す］を押します。

詳しく返す場合：

1. 提出カードを開きます。
2. 本文と画像を確認します。
3. 本文中のほめたい範囲を選び、［選んだ部分にコメント］からスタンプと範囲コメントを付けます。
4. 全体コメントとスタンプを入力します。
5. ［保存して返却］を押します。

［まとめて支援・返却］の［一括返却］は、未返却の記録をまとめて返します。既存の下書きがあればそれを使い、なければ👏を付けます。確認画面に表示される件数を確認してから実行してください。

### 8. Gemini AI支援を使う（任意）

1. 児童の本文がGoogle Generative Language APIへ送信されることについて、学校の規程と保護者への説明の要否を確認します。
2. ［クラス設定］の［Gemini AI支援（任意）］へAPIキーとモデル名を入力し、承認チェックをオンにして保存します。
3. 1件の提出では［AIで下書き］、複数件では［AI下書き］または［AIで詳しく］を使います。
4. 生成された文章と範囲提案を確認・修正し、先生が返却します。

APIキーは先生所有の非共有クラス設定ファイルに保存されます。児童本文は先生がAI支援を実行した時だけ、先生のブラウザからGoogle Generative Language APIへ送られます。AI案は未返却の下書きで、自動返却されません。API利用料が発生する場合があります。

### 9. クラスの様子を見る

［心のバイタル］では、今日の提出数、記述量や気持ちの変化、14日間のヒートマップを確認できます。自動検知は診断ではありません。必ず対面の様子と合わせて判断してください。

### 10. データを出力する

［クラス設定］の［データ出力・保全］から次を使えます。

- ［CSVをダウンロード］：日時、児童名、メールアドレス、テーマ、本文、気持ち、おへんじ、スタンプ、返却状況を出力
- ［印刷・PDF］：印刷画面を開き、プリンターまたはブラウザのPDF保存を使用
- ［Driveへ完全バックアップ］：クラス設定、検証済み児童記録、おへんじ、読取り可能な共有画像の複製を先生のDriveへ作成

## 児童の使い方

### 1. クラスへ参加する

1. 先生から受け取った専用URLまたはQRコードを開きます。
2. 学校で使うGoogleアカウントでログインします。
3. 先生に表示する名前とクラス名を確認します。
4. ［このクラスに参加する］を押します。
5. 承認制のクラスでは、先生の承認を待ちます。

児童のDriveにポートフォリオJSONが作成され、先生へ閲覧者として共有されます。共有に成功すると、先生は提出を読めます。学校の共有ポリシーで拒否された場合は、先生または学校管理者へ相談し、許可後に［先生へもう一度届ける］を押してください。

### 2. 参加済みクラスを開く

共通URLから［児童として使う］を選ぶと、児童自身のDriveにある参加済みクラスが一覧表示されます。目的のクラスを押してください。クラスコードを手入力して検索する画面はありません。

### 3. ふりかえりを書く

1. ［今日のテーマ］を読みます。
2. ［自分のことばで書こう］へ入力します。
3. 書くきっかけが必要な時は、次を使います。
   - ［おまかせ書き出し］
   - YWT／KPT／5W1H／3つの気づきなどの型
   - 気持ち／学び／つながり／次へのヒント
4. 画面を広く使いたい時は［広く書く］を押します。［もとに戻す］で通常配置へ戻ります。
5. 今の気持ちを選びます。選ばなくても提出できます。
6. 必要なら［作品や写真をつける］から画像を選びます。
7. ［できた！ 先生にとどける］を押します。

本文、テーマ、気持ちは入力中に開いている画面のメモリへ下書き保持されます。共有GitHub Pagesオリジンの別アプリから児童本文を読まれないよう、既定では端末の永続保存領域には残しません。再読込み、PWA更新、タブ終了で未提出内容は消えるため、長文は途中で提出するか別の学校認可済みツールも併用してください。提出内容は児童自身のDriveへ保存します。

### 4. ふりがなについて

児童画面でアプリが用意した固定の漢字にはふりがなが付きます。児童自身の文章、先生が入力したテーマ、おへんじなど、利用者が入力した文章には自動でふりがなを付けません。

### 5. おへんじと過去の記録を見る

- 新しい返却があると、画面上部へ新着件数が表示されます。
- 新着表示または過去のカードを押すと、全体コメント、スタンプ、本文範囲のおへんじを読めます。
- カレンダーの ● は書いた日、💬 はおへんじがある日です。
- 既読状態はその端末だけに保存され、別端末とは同期しません。
- 1か月以上前の記録があると［過去の自分と対話］が表示され、今の自分からメッセージを保存できます。

## 戻る操作

スマホやタブレットの端末の戻るジェスチャー／ボタン、ブラウザの戻るボタンは、アプリ内の1つ前の階層へ戻るために使えます。

例：

- 広く書く → 通常の記入画面
- 過去の記録 → クラスの児童画面
- 児童のクラス画面 → 参加済みクラス一覧
- 提出の詳細 → 先生の提出一覧
- 先生のクラス画面 → クラス一覧

アプリ内に戻る先がある間は、ブラウザのページ戻りとアプリ内移動が同時に起きないよう履歴を管理します。アプリの最上位まで戻った後にさらに戻ると、通常のブラウザ動作により前のWebページへ移動したり、PWAが終了したりする場合があります。

## アプリとしてインストール・更新する

- 対応ブラウザでインストール可能になると、画面右下へ［アプリを入れる］が表示されます。
- 新しい版が利用可能になると［新しいバージョンがあります］と表示されます。
- 入力途中に勝手に切り替わりません。下書きを確認してから［さいしんにする］を押します。
- PWAはアプリ画面を起動しやすくし、画面資産をキャッシュします。Googleログイン、Drive同期、提出・返却にはネットワークが必要です。

## データを削除・復元する時

現在版には運営者の中央データベースや運営者所有の中央バックアップがありません。学期末などに先生が［Driveへ完全バックアップ］を実行し、学校の保存ルールに従って管理してください。

- 児童の提出は児童所有、先生のクラス設定と返却は先生所有です。
- Drive上のファイルを所有者が削除すると、アプリからも見えなくなります。
- 誤って削除した場合は、所有者のGoogle Driveのゴミ箱から復元してください。
- アプリの画面には、クラスや全提出を完全削除する管理機能はありません。

## エラーが出た場合

| 表示・症状 | 確認すること |
| --- | --- |
| 学校のGoogle Workspace設定により、この操作が許可されていません | Workspace管理者へ、共通OAuthクライアントID、`drive.file`、`drive.readonly`、必要なDrive共有の許可を依頼する |
| 共有された記録の同期を許可する画面が出る | 説明を確認してボタンを押し、GoogleのDrive閲覧権限を許可する。学校の規則で選べない場合は管理者へ相談する |
| 児童の提出が先生に表示されない | 先生が対象クラスを開き［↻ 最新に更新］を押す。児童側に共有エラーがないか、児童→先生のDrive共有が許可されているか確認する |
| 先生のおへんじが児童に表示されない | 先生が児童を承認済みか、先生→児童のDrive共有が許可されているか確認する。児童側でクラス一覧から開き直す |
| 先生へ共有できない | 学校のDrive共有ポリシーを確認し、許可後に児童画面の［先生へもう一度届ける］を押す |
| Googleへの接続期限が切れた | 未提出の本文を必要に応じて別の学校認可済みツールへ退避し、［Googleアカウントで続ける］から再認可する |
| QRコードを読み込めない | ［専用URLをコピー］または［招待文をコピー］で配る |
| 更新通知が消えない | 入力中の文章を確認して［さいしんにする］を押す。改善しなければタブ／PWAを閉じて共通URLを開き直す |
| 別のGoogleアカウントが使われている | 画面右上のアカウント表示を押して［アカウントを変更］する |

組織ポリシーによるOAuth禁止やDrive共有禁止は、アプリ側で回避できません。個人アカウントでは動き、学校アカウントでだけ失敗する場合は、まずWorkspace管理者へ設定確認を依頼してください。
