# 📖 ふりかえりジャーナル 操作マニュアル

## 先生

### クラスを作る

1. 共通URLを開き、Googleアカウントでログインします。
2. ［先生として使う］を選びます。
3. 初回だけ、説明を確認して［共有された記録の同期を許可する］を押します。児童が別アカウントから共有した提出を受け取るために必要です。
4. クラス名を入力して［クラスを作成］を押します。

先生のDriveに非共有のクラス設定ファイルが作成されます。

### 児童を招待する

1. クラスを開き、［クラス設定］を選びます。
2. 次のうち、教室に合う方法を選びます。
   - 電子黒板や印刷物：QRコードを表示し、必要ならSVG画像で保存
   - Classroomや連絡ツール：専用URLまたは招待文をコピー
   - 確認用：クラスコードをコピー
3. ［児童画面を確認］で、配る前に参加画面を別タブで確認できます。
4. Google Classroom、学習支援ツール、印刷物、電子黒板などで配ります。

クラスコードは確認用です。中央レジストリを置かないため、コードだけでクラスを検索することはできません。通常は専用URLまたはQRを使います。また、一度配った招待URLを中央で強制失効させる機能はありません。参加承認を有効にし、意図しない申請は名簿で除外してください。

### 参加を承認する

児童が専用URLから参加すると、名簿に［承認待ち］として表示されます。［承認］を押すと、先生のDriveにその児童専用のおへんじチャンネルが作られ、児童へ閲覧共有されます。

児童のメールアドレスを先に名簿へ追加しておくと、参加時に自動的に参加中になります。1人ずつ追加するほか、［名簿をまとめて追加］へ `名前, メールアドレス` を1行ずつ貼り付けられます。

### 複数のクラスを切り替える

クラス画面右上の［表示するクラス］から直接切り替えられます。次回［先生として使う］を開くと、同じ端末では前回のクラスへ戻ります。［＋ クラス一覧・新規作成］で一覧や新規作成へ移動します。

### テーマを配信する

［ジャーナル管理］で、適用日と当日のテーマを保存します。曜日別テーマも設定できます。保存すると、参加中の児童別チャンネルへ反映されます。

### 提出を確認して返却する

1. 提出率の円グラフを確認します。別端末からの提出は30秒ごとに安全に確認されます。すぐ確認する場合は［最新に更新］を押します。
2. ［すべて／未返却／返却済み］と検索を使って記録を絞ります。
3. 短い返却なら、カード下の 😊／👏／💪／⭐ を押してすぐ返せます。返却後は取り消しもできます。
4. 詳しく返す場合は提出カードを開きます。
5. 本文と画像を確認します。
6. 本文中のほめたい範囲を選び、［選んだ部分にコメント］から範囲別のスタンプとコメントを付けます。
7. 全体コメントとスタンプを入力します。
8. ［保存して返却］を押します。

Gemini APIキーをクラス設定へ保存している場合、1件の［AIで下書き］と、提出一覧の［AI下書き］［AIで詳しく］を使えます。詳細版は本文中の具体的な箇所も提案します。AI案は「未返却」の下書きとして保存され、そのまま返却されません。内容と児童への適切さを先生が確認・修正してから返却してください。API利用料が発生する場合があります。

［一括返却］は未返却の記録をまとめて返します。既存の下書きがある場合はそれを使い、ない場合は👏を付けます。実行前の確認画面で件数を必ず確認してください。

### クラスの様子を見る

［心のバイタル］では、今日の提出数、記述量や気持ちの変化、14日間のヒートマップを確認できます。自動検知は診断ではありません。必ず対面の様子と合わせて判断します。

### データを出力する

［クラス設定］からCSVダウンロード、印刷、PDF保存ができます。

## 児童

### クラスへ参加する

1. 先生から受け取った専用URLまたはQRを開きます。
2. 学校で使うGoogleアカウントでログインします。
3. 初回だけ、説明を確認して［共有された記録の同期を許可する］を押します。先生が別アカウントから共有するテーマやおへんじを受け取るために必要です。
4. 先生に表示する名前を確認し、［このクラスに参加する］を押します。
5. 承認制のクラスでは先生の承認を待ちます。

### ふりかえりを書く

1. 今日のテーマを読みます。
2. ノート欄へ自分の言葉で書きます。必要なら次を使えます。
   - ［おまかせ書き出し］
   - YWT／KPT／5W1H／3つの気づきの型
   - 気持ち／学び／つながり／次へのヒント
3. 今の気持ちを選び、作品や写真を必要に応じて追加します。
4. ［できた！ 先生にとどける］を押します。

入力途中の本文は、その端末だけに下書き保存されます。提出後に下書きは削除されます。

### おへんじを見る

先生が返却すると、画面上部へ新着のおへんじ件数が表示されます。通知または過去のカードを押すと、全体コメントと、先生が文章中に付けたスタンプ・コメントを読めます。カレンダーの ● は書いた日、💬 はおへんじがある日です。既読状態はその端末内だけに保存されます。

1か月以上記録がたまると、過去の自分の文章が表示され、今の自分からメッセージを残せます。

## アプリとしてインストール・更新する

- 対応ブラウザでインストール可能になると、画面右下へ［アプリを入れる］が表示されます。
- 新しい版が利用可能になると［新しいバージョンがあります］と表示されます。入力途中に勝手に切り替わることはありません。下書きを確認してから［さいしんにする］を押します。
- GoogleログインとDrive通信にはネットワークが必要です。PWAはアプリ画面を起動しやすくするもので、提出データを端末だけに保存する仕組みではありません。

## エラーが出た場合

| 表示 | 確認すること |
| --- | --- |
| Workspace設定により許可されていない | 学校管理者に共通OAuthクライアントIDの許可を依頼する |
| 共有記録の同期が許可されていない | Googleの権限画面でDriveの閲覧許可を選ぶ。学校アカウントでは管理者に `drive.readonly` の許可を依頼する |
| 先生へ共有できない | 学校ドメイン内のDrive共有ポリシーを確認し、［先生へ再共有］を押す |
| QRを読み込めない | 専用URLまたは招待文をコピーして配る |
| ログイン期限切れ | 再度Googleログインする。入力済み下書きは同じ端末に残る |
| 更新通知が消えない | 入力中の文章を確認し、［さいしんにする］を押す。改善しなければタブを閉じて共通URLを開き直す |
