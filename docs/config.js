// ============================================================
// ふりかえりジャーナル — PWAシェル設定
// ============================================================
// appUrl に GAS Webアプリの exec URL（https://script.google.com/macros/s/.../exec）
// を書くと、全ユーザー共通の接続先として固定されます。
// 空文字のままにすると、初回アクセス時に画面上で URL を入力してもらい、
// localStorage に保存します。
window.APP_CONFIG = {
  appUrl: ""
};
